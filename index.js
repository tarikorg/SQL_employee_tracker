require('console.table')//to view sql data in an appealing way i guess
const inquirer = require('inquirer')
const { startApp, promptAddDepartment, promptAddRole, promptAddEmployee, promptUpdateEmployee } = require('./interface')

startApp()







